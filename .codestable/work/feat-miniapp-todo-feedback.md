# TODO 动态配置与反馈入口

## 目标
按用户最新要求，本次仅保留“码是配置的”和“反馈入口”，其他改动还原。

## 现场
2026-09-11，dev 分支，基线 56e95e1。用户最终明确授权 push 和上传：已提交并推送 c706bbb4，小程序开发版本 0.1.63.20260911 的微信 CLI 上传成功，详见 [交付记录](todo-release-63.md)。无关语音和键盘改动保留，未部署后端。

## 边界
恢复 app.go HTTP Handler 与 application/wechatminiapp 的原实现；删除拦截器、来源登记、新会话表模型与测试。原登录、Token、业务权限和 Web 请求不增加逻辑或查询。保留原五张 TODO 表的建表逻辑，不删除任何历史数据。

## 保留行为
后台参数 miniapp_todo:feedback_code 默认空，最多 128 个字符，敏感加密保存，提交反馈时读取最新值。只保留一个反馈输入框，错误内容提示反馈成功，匹配时永久记录身份并导航；已有授权保留，不保存反馈正文。删除旧独立 unlock 路由和相关客户端入口，同步 Swagger/TypeScript 契约。

## 验证
服务、参数、登录、HTTP handler、错误码/i18n 测试通过；10 项 TODO 客户端和页面测试通过；API 契约已生成且 api:check 通过。回退检查确认 app.go、微信登录文件和 TODO 表模型与 HEAD 无差异；搜索确认无拦截器或来源登记引用残留。迁移/启动测试发现需要保留原五表建表逻辑，恢复后 infra/persistence/miniapptodo 和 internal/app 全部通过。git diff --check 通过。

## 状态
功能收敛、提交和开发版上传已完成，迁移/启动、API 一致性验证及该提交的三项 GitHub 检查通过。本次主 agent 执行，无子 agent、无独立审查。当前没有进行中的上传或需要恢复的运行脚本。
